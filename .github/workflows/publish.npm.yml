name: 'Publish packages'

on:
  push:
    tags:
      - "v[0-9]+(\\.[0-9]+)*"

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    uses: zondax/_workflows/.github/workflows/_publish-npm.yaml@v7
    permissions:
      contents: read
      id-token: write
